# Base URL of the backend API. In dev the Vite proxy forwards /api to the
# backend, so the default works. In production set this to your API origin,
# e.g. https://api.yourpractice.com
VITE_API_BASE_URL=/api

# Used only by the dev server proxy target
VITE_API_PROXY=http://localhost:4000
